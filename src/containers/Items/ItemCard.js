import React from 'react';
import ItemCardList from './ItemsCardList';
import './style.css';

const ItemCard = (itemsData) => (
  <ul>
    <ItemsCardList itemsData={this.state.itemsData.map()} />
  </ul>
);

export default ItemCard;
